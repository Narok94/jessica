rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ===============================================================
    // Assumed Data Model
    // ===============================================================
    //
    // Collection: exercise_gifs
    // Document ID: normalized exercise name
    // Fields:
    //   - url: string (required, uri)
    //   - updatedAt: string (required, date-time)
    //
    // Collection: users
    // Document ID: userId (uid)
    // Fields:
    //   - username: string
    //   - name: string
    //   - role: string (student, teacher)
    //   - streak: number
    //   - totalWorkouts: number
    //
    // ===============================================================

    // ===============================================================
    // Helper Functions
    // ===============================================================
    
    function isAuthenticated() {
      return request.auth != null;
    }
    
    function isAdmin() {
      return isAuthenticated() && 
        (get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'teacher');
    }
    
    function isOwner(userId) {
      return isAuthenticated() && request.auth.uid == userId;
    }

    function hasOnlyAllowedFields(fields) {
      return request.resource.data.keys().hasOnly(fields);
    }

    // ===============================================================
    // Domain Validators
    // ===============================================================

    function isValidGif(data) {
      return hasOnlyAllowedFields(['url', 'updatedAt']) &&
             data.url is string && data.url.size() < 1000 &&
             data.updatedAt is string;
    }

    function isValidUser(data) {
      return hasOnlyAllowedFields(['username', 'name', 'role', 'streak', 'totalWorkouts', 'checkIns', 'history', 'isProfileComplete', 'password', 'age', 'goal', 'weights', 'badges', 'uid']) &&
             data.username is string && data.username.size() < 50 &&
             data.name is string && data.name.size() < 100 &&
             data.role in ['student', 'teacher'];
    }

    // ===============================================================
    // Rules
    // ===============================================================

    match /exercise_gifs/{exerciseId} {
      allow read: if true; // Publicly readable so all users see GIFs
      allow write: if isAdmin() && isValidGif(request.resource.data);
    }

    match /users/{uid} {
      allow read, update: if isAuthenticated() && (request.auth.uid == uid || isAdmin());
      allow create: if isAuthenticated() && request.auth.uid == uid && isValidUser(request.resource.data);
      allow delete: if isAdmin();
    }

    match /usernames/{username} {
      allow get: if true; // Allow looking up UID by username
      allow create, update: if isAuthenticated(); // Simple for now
    }

    // Default deny
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
